// Package aggregator implements the MCP aggregation core: it merges upstream capabilities, applies
// server-name prefixing, and routes client requests to the owning upstream server.
package aggregator
