# Build artifacts
/bin/
coverage.out
coverage.html

# Generated Templ output (regenerated via `make templ`)
*_templ.go

# Local data / config
*.db
*.db-wal
*.db-shm
garmx.json.local

# Local Claude Code settings (per-user, not shared)
.claude/settings.local.json
.claude

# Stray probe capture logs
mcpprobe.log

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
